export type DesktopApplication = "about" | "contact" | "projects" | "memory";

export default DesktopApplication;
export const desktopApplications: DesktopApplication[] = [
  "about",
  "contact",
  "projects",
  "memory",
];
